<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.nikolas.spotfiyusefirsttry">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyTheme"

        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".PlaylistSelect" />
        <activity android:name=".PlayQuiz" />
        <activity
            android:name=".MainAppActivity"
            android:label="@string/title_activity_main_app"
            android:theme="@style/MyTheme" >
        </activity>
        <activity android:name=".QuizSummary" />
        <activity android:name=".SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity"/>
        <activity android:name=".QuizLobbyActivity" />
    </application>

</manifest>